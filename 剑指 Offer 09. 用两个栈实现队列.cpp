#if 0
#include <iostream>
#include <stack>

using namespace std;

/*
class CQueue {
private:
    stack<int> a;
    stack<int> b;
public:
    CQueue() {
    }

    void appendTail(int value) {
        a.push(value);
    }

    int deleteHead() {
        if (b.empty())
        {
            while (!a.empty())
            {
                b.push(a.top());
                a.pop();
            }
        }
        if (b.empty()) return -1;
        else
        {
            int tmp = b.top();
            b.pop();
            return tmp;
        }
    }
};

*/


int main()
{
    
}
#endif